import 'package:flutter/material.dart';

class AppColors {
  AppColors._(); // prevents instantiation

  /// 🌿 Primary Theme Colors
  static const Color primary = Color(0xFF4E9AA7);
  static const Color primaryDark = Color(0xFF4B8E94);
  static const Color accent = Colors.teal;
  static const Color white = Colors.white;
  static const Color chatBubble = Color(0xFFD9EEF0);
  static const Color inputBg = Color(0xFFE8F4F5);
  static const Color textPrimary = Colors.black;
  static const Color textSecondary = Colors.grey;
  static const Color textLight = Colors.white70;
  static const Color border = Colors.teal;
  static const Color icon = Colors.teal;
  static const Color loader = Colors.white;
  static const Color sendButton = Colors.teal;
}
